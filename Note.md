#1.多线程编程

在异步编程中，实质上是将整个程序变成了一个事件循环，代码中的异步协程被轮流唤醒并工作(每时每刻程序只干一件事)，最后完成任务。实际上的CPU行为是必须在await发生后才会打断某一过程切入下一过程，原子操作（不会被打断分割/改写顺序）是任意的await之间的部分

在多进程中，不同线程间事件的执行将不再有严格的顺序规则，而是存在竞争关系，所以说对于没有进程保护的变量来说跨线程修改是十分危险的，因为你不能确定一个修改流程是否走完（这个时候的原子操作只有最基层的C赋值和判断还有计算）

多线程没学过，整体上和多进程类似，但是不再共享变量，通讯需要通过专门的跨进程通信协议/手动维护特定队列。

